[delta]
    # plus-style = "syntax #72c270"
    # minus-style = "syntax #ad6163"
    line-numbers = true
    side-by-side = true
    # Navigate by diff (with `n` and `N`)
    navigate = true
    zero-style = dim syntax
    hyperlinks = true
    paging = always
    pager = bat
