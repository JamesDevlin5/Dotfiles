
function __pwd() {
  local RELATIVE=$(pwd)
  local PHYSICAL=$(pwd -P)
  if [[ $RELATIVE != $PHYSICAL ]]; then
    print -P "\t- %{%F{yellow}%G}$RELATIVE%{%f%G}"
    print -P "phys- %{%F{cyan}%G}$PHYSICAL%{%f%G}"
  else
    print -P "\t- %U$RELATIVE%u"
  fi
}

__pwd
# vim:filetype=zsh
